{
  "name": "kuanhouli-gas-pipeline-map",
  "private": true,
  "scripts": {
    "build": "vite build",
    "build:pages": "npm run build && node scripts/prepare-github-pages.js",
    "dev": "vite --host 127.0.0.1",
    "export:poster": "node scripts/export-poster.js",
    "serve": "node scripts/export-server.js"
  },
  "devDependencies": {
    "playwright": "^1.61.0"
  },
  "dependencies": {
    "@amap/amap-jsapi-loader": "^1.0.1",
    "dotenv": "^17.4.2",
    "proj4": "^2.20.9",
    "three": "^0.142.0",
    "vite": "^8.0.16"
  }
}
